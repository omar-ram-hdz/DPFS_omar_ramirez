---
import Layout from "@/layouts/Layout.astro";
import HeaderLogo from "@/components/header/HeaderLogo.astro";
import Button from "@/components/site/Button.astro";
import Input from "@/components/site/Input.astro";
import Anchor from "@/components/site/Anchor.astro";
---
<Layout title="Iniciar sesión en Amantoli - Productos artesanales a precios accesibles">
  <HeaderLogo/>
  <main>
    <form action="/users" method="get">
      <h1>Crear cuenta</h1>
      <Input
        name="nombre"
        type="text"
        placeholder="Juanita"
      >
        Nombre(s):
      </Input>
      <Input
        name="apellidos"
        type="text"
        placeholder="Davila Marcial"
      >
        Apellidos:
      </Input>
      <Input
        name="email"
        type="email"
        placeholder="correo@ejemplo.com"
      >
        Correo:
      </Input>
      <Input
        name="password"
        type="password"
        placeholder="hola1234"
      >
        Contraseña:
      </Input>
      <Button type="submit" hasFull={true}>Crear cuenta</Button>
      <p>¿Ya tienes cuenta? <Anchor href="/login">Iniciar Sesión</Anchor></p>
    </form>
  </main>
</Layout>

<style>
  main{
    padding: 200px 200px;
    color: var(--black);
  }
  h1{
    color: var(--red);
    margin-bottom: 1ch;
  }
  form{
    margin: auto;
    max-width: 600px;
    padding: 40px 20px;
    background-color: var(--backWhite);
    border-radius: 10px;
  }
  form p{
    margin-top: 1ch;
    text-align: center;
  }
</style>